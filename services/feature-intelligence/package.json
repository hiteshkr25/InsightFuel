{
  "name": "@insightfuel/feature-intelligence",
  "version": "1.0.0",
  "private": true,
  "description": "InsightFuel Feature Intelligence Processing Engine",
  "scripts": {
    "build": "echo 'No build required for Python'",
    "dev": "uvicorn app.main:app --reload --port 3005",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 3005",
    "lint": "echo 'No lint required for Python'",
    "test": "python -m pytest",
    "clean": "rm -rf .pytest_cache __pycache__ app/__pycache__"
  }
}
