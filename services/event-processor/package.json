{
  "name": "@insightfuel/event-processor",
  "version": "1.0.0",
  "private": true,
  "description": "InsightFuel Event Processing Pipeline Worker Service",
  "scripts": {
    "build": "echo 'No build required for Python'",
    "dev": "uvicorn app.main:app --reload --port 3002",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 3002",
    "lint": "echo 'No lint required for Python'",
    "test": "python -m pytest",
    "clean": "rm -rf .pytest_cache __pycache__ app/__pycache__"
  }
}
