{
  "name": "@insightfuel/utils",
  "version": "1.0.0",
  "private": true,
  "description": "Shared utility helpers",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/**/*.ts",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@insightfuel/typescript-config": "workspace:*",
    "typescript": "^5.4.2",
    "vitest": "^1.4.0"
  }
}
